import BackofficeLayout from "@/components/backofficeLayout";
import { Typography } from "@mui/material";

const BackofficeApp = () => {
  return (
    <BackofficeLayout>
      <Typography variant="h4">Backoffice Home Page</Typography>
    </BackofficeLayout>
  );
};

export default BackofficeApp;
